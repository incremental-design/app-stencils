{
  "$schema": "http://json-schema.org/schema",
  "version": 2,
  "cli": "nx",
  "title": "Build executor",
  "description": "",
  "type": "object",
  "properties": {
    "outputPath": {
      "type": "string",
      "description": "Where to output the built package"
    },
    "bugs": {
      "type": "string",
      "description": "Where users of this package should report bugs"
    }
  },
  "required": [
    "outputPath"
  ]
}