{
  "$schema": "http://json-schema.org/schema",
  "cli": "nx",
  "$id": "TypescriptLibrary",
  "title": "",
  "type": "object",
  "properties": {
    "name": {
      "type": "string",
      "description": "",
      "$default": {
        "$source": "argv",
        "index": 0
      },
      "x-prompt": "What name would you like to use?"
    },
    "tags": {
      "type": "string",
      "description": "Add tags to the project (used for linting)",
      "alias": "t"
    },
    "directory": {
      "type": "string",
      "description": "A directory where the project is placed"
    },
    "description": {
      "type": "string",
      "description": "what the library does. How it is distinct from the other libraries in this monorepo",
      "x-prompt": "What does this library do?"
    },
    "bugs": {
      "type": "string",
      "description": "url of place that consumers of this package can report bugs. This will be added to the package.json 'bugs' field if it is provided.",
      "x-prompt": "Where should users go to report bugs?"
    },
    "repositoryUrl": {
      "type": "string",
      "description": "url where code for this repo can be viewed",
      "x-prompt": "Where is this repository located online?"
    }
  },
  "required": [
    "name"
  ]
}