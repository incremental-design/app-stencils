export * from './event-handlers';

// make a default export with all the methods.

/**
 * handle is a wrapper for every other event handler in this package. When you insert an event into it, it runs any of
 *
 * Block Tags
 *
 */
export default function handle() {}

// add the shape + usage instructions for event listeners to readme config. make sure to explain that every event listener has the same kind of signature: it accepts a previous event
