{
  "name": "root",
  "private": true,
  "type": "module",
  "scripts": {
    "test:unit": "vue-cli-service test:unit",
    "prepare": "husky install",
    "postinstall": "lerna bootstrap --force-local --hoist",
    "build": "lerna run build",
    "publish:snapshot": "lerna publish --canary",
    "create-package": "./createPackage.js",
    "clean": "lerna clean"
  },
  "devDependencies": {
    "@commitlint/cli": "^12.1.4",
    "@commitlint/config-conventional": "^12.1.4",
    "@typescript-eslint/eslint-plugin": "^4.4.1",
    "husky": "^6.0.0",
    "lerna": "^5.0.0",
    "typescript": "latest",
    "@volar-plugins/prettier": "latest"
  }, 
  "workspaces": [
    "packages/shared/*", "packages/vue3/*", "packages/storybook"
  ]
}
